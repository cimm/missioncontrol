.clearfix
  = form.label :name
  .input
    = form.text_field :name
.clearfix
  = form.label :client_id
  .input
    = form.select :client_id, @clients.collect {|c| [c.nickname, c.id]}
.clearfix
  = form.label :default_rate
  .input
    .input-prepend
      %span.add-on €
      = form.text_field :default_rate, :class => "input-mini"
