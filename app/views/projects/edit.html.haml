%section#edit-project
  .page-header
    %h1 Edit project
  = form_for @project do |f|
    = render :partial => "form", :object => f
    .actions
      = f.submit "Update project", :class => "btn primary"
      or
      = link_to "cancel", projects_path
