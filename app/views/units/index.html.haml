%h1 Units

= form_for @unit do |f|
  = f.date_select :executed_at
  = f.submit "Add unit", :class => "btn primary"

%table.zebra-striped
  %thead
    %tr
      %th Executed at
      %th
  %tbody
    - @units.each do |unit|
      %tr
        %td.executed_at= unit.executed_at
        %td= link_to "edit", edit_unit_path(unit), :id => "edit_unit_#{unit.id}", :class => "btn small"

%p
  Total:
  = @total_units
  units
