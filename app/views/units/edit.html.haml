%section#edit-unit
  .page-header
    %h1 Edit unit
  = form_for @unit do |f|
    = render :partial => "form", :object => f
    .form-actions
      = f.submit "Update unit", :class => "btn btn-primary"
      or
      = link_to "cancel", units_path
