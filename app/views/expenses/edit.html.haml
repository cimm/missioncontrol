%section#edit-client
  .page-header
    %h1 Edit expenses
  = form_for @expense do |f|
    = render :partial => "form", :object => f
    .actions
      = f.submit "Update expense", :class => "btn primary"
      or
      = link_to "cancel", expenses_path
